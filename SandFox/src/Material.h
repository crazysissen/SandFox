#pragma once

#include "SandFoxCore.h"
#include "IBindable.h"
#include "Texture.h"

#include <string>

namespace SandFox
{

	//class FOX_API Material
	//{
	//public:
	//	// Empty material
	//	Material(); 

	//	// Material with 4 internally handelled and owned textures. Empty textures are inferred.
	//	Material(std::wstring albedo, std::wstring ambient = L"", std::wstring specular = L"", std::wstring normal = L"");

	//	void Load(std::wstring albedo, std::wstring ambient = L"", std::wstring specular = L"", std::wstring normal = L"");

	//private:
	//	Texture* m_albedo;
	//};

}
